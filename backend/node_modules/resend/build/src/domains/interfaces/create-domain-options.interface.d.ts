import { PostOptions } from 'resend/build/src/common/interfaces';
import { ErrorResponse } from 'resend/build/src/interfaces';
import { Domain, DomainRecords, DomainRegion } from 'resend/build/src/domains/interfaces/domain';
export interface CreateDomainOptions {
    name: string;
    region?: DomainRegion;
}
export interface CreateDomainRequestOptions extends PostOptions {
}
export interface CreateDomainResponseSuccess extends Pick<Domain, 'name' | 'id' | 'status' | 'created_at' | 'region'> {
    records: DomainRecords[];
}
export interface CreateDomainResponse {
    data: CreateDomainResponseSuccess | null;
    error: ErrorResponse | null;
}
