import { ErrorResponse } from 'resend/build/src/interfaces';
import { Domain, DomainRecords } from 'resend/build/src/domains/interfaces/domain';
export interface GetDomainResponseSuccess extends Pick<Domain, 'id' | 'name' | 'created_at' | 'region' | 'status'> {
    object: 'domain';
    records: DomainRecords[];
}
export interface GetDomainResponse {
    data: GetDomainResponseSuccess | null;
    error: ErrorResponse | null;
}
